package main

import (
	"fmt"
	"math"
	//Import using github.com account path i.e. github.com/jean530/project_name/package_name
)


func main() {
	fmt.Println(math.Floor(math.Pi))
	fmt.Println(math.Ceil(math.Pi))
	fmt.Println(math.Sqrt(math.Pi))
	//.Println(strutil.Reverse("erzura sykcor"))
}